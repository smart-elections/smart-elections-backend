version: 0.2

phases:
  install:
    commands:
      - echo "installing files"
  pre-build:
    commands:
      - echo "pre-build phase"

  build:
    commands:
      - echo "build block"
      - echo "running tests"
  post_build:
    commands:
      - echo "post build phase"
